from setuptools import setup

setup(name='learning_dashboard',
      version='0.0.1',
      install_requires=['numpy',
                        'imageio',
                        'requests']
      )  
